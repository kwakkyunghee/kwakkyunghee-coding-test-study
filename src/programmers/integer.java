package programmers;

public class integer {

    public int solution(String s) {
        int answer = 0;
        return Integer.valueOf(s);
        //integer 클래스안에 valueOf메소드가 만들어져있음.
        //s를 int로 바꿔서 return 해줌.
    }
}
